const initialState = {
    profile: [],
    project: [],
    mainPosts: [],
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;